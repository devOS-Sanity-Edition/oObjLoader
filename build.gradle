plugins {
    id 'java'
}

group 'com.owens'
version '1.0-alpha.4'

repositories {
    mavenCentral()
}

dependencies {
    // h
}

task sourcesJar(type: Jar, dependsOn: classes) {
    classifier = 'sources'
    from sourceSets.main.allSource
}

test {
    useJUnitPlatform()
}